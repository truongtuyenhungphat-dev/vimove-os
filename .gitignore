# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# env files
.env
.env*.local

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# prisma
/prisma/dev.db
/prisma/dev.db-journal

/app/generated/prisma

.vercel
.env*

# bundled skill assets' own build/test artifacts (Python) — not source content
__pycache__/
*.pyc
.coverage
coverage-ui.json

# Raw customer PII exported from the legacy Firebase system for the one-time
# Phase 15 warranty migration (scripts/migrate-warranty-data.ts) — real names/
# phones/emails/addresses. This repo is PUBLIC on GitHub; these must never be
# committed. Kept locally only, deleted once the migration has run against
# production.
scripts/data/legacy-*.json

.vercel
.env*
